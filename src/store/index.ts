import { configureStore } from '@reduxjs/toolkit'

const INITIAL_STATE = {
  comics: []
}

function reducer(state = INITIAL_STATE, action: AnyAction) {
  switch (action.type){
    case  'GET_COMICS_LIST'[

    ]
  }
  return []
}

const store = configureStore({reducer:{

}
})

export default store
